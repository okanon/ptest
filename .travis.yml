dist: bionic

matrix:
   include:
      - os: linux
        compiler: gcc
        env: C=gcc
      - os: linux
        compiler: clang
        env: C=clsng


script:
   - git clone https://github.com/okanon/tbd.linux && tbd.linux
   - make 
